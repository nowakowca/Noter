# Copy this file to `.env` to customize where backups are saved.

# Host directory where Instagram backups are written.
# Point this at any folder you like — the app writes <username>_<N>.jpg files
# into per-profile subfolders here.
# Defaults to ./backups (next to docker-compose.yml) if unset.
MEDIA_PATH=./backups
