{
  "name": "noter",
  "version": "0.1.0",
  "description": "A simple note-taking / organization app for managing a list of items with a name, hyperlink, description and completion status.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  }
}
